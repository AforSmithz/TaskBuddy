# Build output for the web Lambda. Regenerated by aws/scripts/build-web.sh;
# 41 MB of compiled Next.js has no business in git.
.build/

# CDK synth output.
infra/cdk.out/
infra/node_modules/

# Written at runtime by the tsx harness to stub Next's `server-only` alias,
# which plain Node cannot resolve.
harness/.server-only-shim.js
